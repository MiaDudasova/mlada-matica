const Loading = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "80.5vh",
        backgroundColor: "var(--bg)",
        color: "var(--blue)",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Načítava sa...
    </div>
  );
};

export default Loading